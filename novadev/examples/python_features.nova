# Run from the project root:
#   python nova.py run novadev/examples/python_features.nova

let name = "NovaDev"
print("Hello {name} 0.4")

let items = ["lexer", "parser", "runtime"]
items.add("generator")

for item in items {
    print(item)
}


let thing="thing"
print("hello{thing}-thing")

let thing = ["aldane","monkey","thingt"]
thing.add("yoyo")

for item in thing {
    print(thing)
}