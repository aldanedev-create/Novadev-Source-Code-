import models.*
import pages.*
import workflows.*
import modules.*

app ChurchCustomOneOne {
    project {
        frontend Vue
        backend Flask
        database SQLite
        structure VueFlask
        mode custom
    }
}
