# hello-ui

This is a tiny example NovaDev package.

Install it locally:

```bash
python novapm.py install packages/hello-ui
```

The package contains a reusable `.nova` page declaration. Real packages can
contain modules, pages, workflows, custom frontend code, backend helpers, docs,
and starter app declarations.
