# DashboardKit

DashboardKit is a NovaDev package with starter dashboard declarations.

It includes:

- a clean dashboard theme
- an admin dashboard page
- navbar and card examples
- a small helper function

Install it with:

```bash
novapm install dashboard-kit
```
