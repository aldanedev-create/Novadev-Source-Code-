# AuthKit

AuthKit is a starter NovaDev package for authentication-focused projects.

It includes:

- an `AuthUser` table
- an `auth AuthUser` declaration
- a protected `/api/me` route idea
- a small helper function

Install it with:

```bash
novapm install auth-kit
```
